# WEDORA — Builder Action Fix (2026-08-22)

الإصدار ده يقفل مشكلة إن الواجهة تظهر لكن الضغط على أزرار الـBuilder ما يعملش حاجة.

## اللي اتغير

- ربط أزرار الـBuilder **قبل** تحميل الداتا والـtemplates؛ لو حصل خطأ أثناء boot تفضل الأزرار شغالة.
- ربط آمن بدون ما عنصر ناقص واحد يوقف بقية الـhandlers.
- إصلاح `إضافة` للأقسام المخصصة مع scroll/highlight للقسم الجديد.
- Feedback واضح عند `حفظ المسودة` / `التجربة` / `اختيار الخطة` بدل الضغط الصامت.
- Demo Mode المحلي يدخل كـ Demo Customer تلقائيًا عشان تختبر Save / Trial / Pricing فورًا.
- تنظيف state القديم القادم من V6/V7/V8/V9 قبل استخدامه.
- منع `blob:` و`data:` URLs القديمة من ملء localStorage وتعطيل الحفظ.
- fallback عند امتلاء localStorage بدل توقف زر الحفظ بالكامل.
- Cache-busting للـBuilder CSS/JS عشان المتصفح ما يحملش نسخة JavaScript قديمة.

## اختبار محلي سريع

1. افتح `builder.html` من Local Server.
2. جرّب `+ إضافة` — لازم يظهر كارت القسم مباشرة.
3. اضغط `حفظ المسودة` — لازم يظهر `تم حفظ المسودة ✓` ويتحول الرابط إلى `builder.html?id=...` في Demo Mode.
4. اضغط `جرّب 24 ساعة` — لازم يفتح الدعوة التجريبية.
5. ارجع واضغط `اختار خطة` — لازم يفتح صفحة الباقات.
