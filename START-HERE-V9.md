# WEDORA V9 — ابدأ من هنا

## لو المشروع الحالي شغال عندك بالفعل
اعمل حاجة واحدة في قاعدة البيانات:

1. Supabase → SQL Editor
2. شغّل: `sql/RUN-THIS-ONE-V9.sql`
3. اعمل Redeploy للملفات الجديدة على Vercel.
4. افتح حساب العميل واضغط **تحديث حالة اشتراكي** مرة واحدة.

> لا تشغّل ملفات SQL القديمة واحد واحد. موجودة فقط داخل `sql/legacy/` للرجوع لها.

## لو بتعمل مشروع Supabase جديد من الصفر
شغّل فقط:

`sql/INSTALL-FRESH-V9.sql`

ثم عدّل البريد داخل `sql/MAKE-ADMIN.sql` وشغّله لإنشاء Role الأدمن لحساب Auth الذي أنشأته.

## الـFlow الجديد البسيط

### العميل
اختار قالب → عدّل → احفظ → انشر → Share Center

لو الحساب غير مشترك:
اختار خطة → ارفع الإيصال → الأدمن يعتمد → الباقة تظهر تلقائيًا في Builder → انشر.

### الأدمن
Payment Queue → افتح الإيصال → Approve

Approve يفعّل **باقة الحساب** ولا ينشر مسودة ناقصة تلقائيًا. العميل هو الذي يضغط نشر عندما يخلص.

## المشاركة
بعد النشر تفتح `share.html` تلقائيًا وفيها:
- نسخ الرابط
- Share من الموبايل
- WhatsApp
- فتح الدعوة
- QR Code
- الرجوع للتعديل

روابط Vercel المنشورة تكون بالشكل:
`https://YOUR-DOMAIN/w/your-slug`

و`vercel.json` يتولى توجيهها للدعوة.

## لو العميل دفع قبل V9
`RUN-THIS-ONE-V9.sql` يعيد بناء الاشتراك تلقائيًا من كل Order حالته `approved`.
كمان الـBuilder يفحص الاشتراك عند فتح الصفحة، وعند رجوع المستخدم للتاب، وكل 12 ثانية لو مازال غير ظاهر.
